// Solution-1.scala
// Solution to Exercise 1 in "References & Mutability"
import com.atomicscala.AtomicTest._

/*{oldDescription}  Create a var reference to an immutable Map and
demonstrate what this means (That is, prove that you can't change
its contents, nor append to it. Show that you can rebind the
reference). Now create a val reference to a mutable Map and
demonstrate what this means. {oldDescription}*/

assert("Solution" == "Incomplete")
